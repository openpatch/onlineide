export class MainTimer {
    constructor(f, timeInMs) {
        this.f = f;
        this.timeInMs = timeInMs;
        this.stopped = false;
    }
    start() {
        let lastTime = performance.now();
        let f1 = (time) => {
        };
    }
}
//# sourceMappingURL=MainTimer.js.map